{
	"name": "obsidian-duplicate-tab",
	"version": "1.0.0",
	"description": "This plugin adds «duplicate tab» functionality to context menu and a respective keyboard shortcut setting.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"keywords": [],
	"author": "",
	"license": "",
	"devDependencies": {
		"builtin-modules": "3.3.0",
		"obsidian": "latest",
		"tslib": "2.4.0",
		"typescript": "4.7.4"
	}
}
